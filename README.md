git push -u origin nombreRama (jose, cristina, antonio, etc) 

Si os peta porque os dice algo de divergencia cuando intentais hacer pull o push, primero leed si os pide hacer merge.
Si os lo pide poneis primero `git config pull.rebase false` y despues `git pull`
Después de eso debería dejar hacer push.
