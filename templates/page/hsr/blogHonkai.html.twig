{% extends 'base.html.twig' %}

{% block title %}GameHub Blog Honkai: Star Rail{% endblock %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('css/styleBlogHONKAI.css') }}">

    {# Estilos específicos para los elementos dinámicos del header en esta página #}
    <style>
        header .login-container {
            display: flex;
            align-items: center;
            gap: 15px;
            background: transparent !important;
            border: none !important;
            box-shadow: none !important;
            padding: 0 !important;
        }

        .user-info {
            display: flex;
            flex-direction: column;
            align-items: flex-end;
            text-align: right;
        }

        .welcome-text {
            color: #c0c0c0; /* Color plateado para HSR */
            font-weight: bold;
            font-size: 0.9rem;
        }

        .logout-link {
            color: #ffffff;
            font-size: 0.75rem;
            text-decoration: underline !important;
            opacity: 0.7;
        }

        .logout-link:hover {
            opacity: 1;
            color: #ffadad;
        }
    </style>
{% endblock %}

{% block body %}

    {# Invocamos el header modular pidiendo el icono SILVER #}
    {% include 'partials/header.html.twig' with {'iconColor': 'SILVER'} %}

    <main>
        <div class="container blog-wrapper">
            <div class="top-actions">
                <a href="{{ path('hsr_nuevo_post', {'juego': 'hsr'}) }}" title="Crear Post">
                    <img src="{{ asset('assets/img/postIconSILVER.webp') }}" alt="Crear Post" class="action-icon">
                </a>

                <a href="{{ path('app_build_hsr_create') }}" title="Crear Build">
                    <img src="{{ asset('assets/img/buildIconSILVER.webp') }}" alt="Crear Build" class="action-icon">
                </a>
            </div>

            <article class="post-card">
                <div class="post-content">
                    <h2>Build Suprema de Raiden Shogun</h2>
                    <p>Aquí explicamos la mejor rotación para el abismo actual. Necesitarás mucha recarga de energía y el set de Emblema del Destino para maximizar el daño de la Musou no Hitotachi.</p>
                </div>

                <div class="post-icons-area">
                    <span class="icon-label">Personajes clave:</span>
                    <div class="icon-grid">
                        <img src="{{ asset('assets/img/char1.png') }}" alt="Icon" class="char-icon">
                        <img src="{{ asset('assets/img/char2.png') }}" alt="Icon" class="char-icon">
                        <img src="{{ asset('assets/img/char3.png') }}" alt="Icon" class="char-icon">
                        <img src="{{ asset('assets/img/char4.png') }}" alt="Icon" class="char-icon">
                    </div>
                </div>
            </article>

            <hr class="divider">

            <article class="post-card">
                <div class="post-content">
                    <h2>Guía de Banner: ¿Tirar o Ahorrar?</h2>
                    <p>El nuevo banner de Honkai Star Rail trae personajes de Cacería muy fuertes. Analizamos si valen la pena tus jades estelares o si deberías esperar a la versión 2.0.</p>
                </div>

                <div class="post-icons-area">
                    <span class="icon-label">Recomendados:</span>
                    <div class="icon-grid">
                        <img src="{{ asset('assets/img/char2.png') }}" alt="Icon" class="char-icon">
                        <img src="{{ asset('assets/img/char1.png') }}" alt="Icon" class="char-icon">
                    </div>
                </div>
            </article>

        </div>
    </main>

    <footer>
        Game Hub &copy; 2025-2026
    </footer>

{% endblock %}